apiVersion: v1
kind: Service
metadata:
  name: {{ include "vrepaas.fullname" . }}-app
  labels:
    {{- include "vrepaas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.vreapp.service.type }}
  ports:
    - port: {{ .Values.vreapp.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vrepaas.selectorLabels" . | nindent 4 }}
